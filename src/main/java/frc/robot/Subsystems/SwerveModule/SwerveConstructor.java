package frc.robot.Subsystems.SwerveModule;

import edu.wpi.first.math.geometry.Translation2d;


public class SwerveConstructor extends SwerveModule {

    public SwerveConstructor(int driveId, int turnId, 
                    String moduleName, Translation2d offset) {
        super(driveId, turnId, moduleName, offset);
    }

}